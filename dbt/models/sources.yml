version: 2

sources:
  - name: raw_weather
    database: analytics
    schema: public
    tables:
      - name: weather_raw
        description: "Raw MongoDB documents loaded into Postgres (EL landing table)"
        columns:
          - name: mongo_id
            tests:
              - unique
              - not_null
          - name: fetched_at
          - name: payload
